Place your resume PDF here (e.g. resume.pdf) once you have one.

Then either:
1. Host it online (Google Drive/Firebase Storage) and set
   AppConstants.resumeUrl in lib/core/constants/app_constants.dart, or
2. Bundle it locally and open it with a package like `open_filex`
   from the Resume screen's download button.
