Place your profile photo and any project screenshots here
(e.g. profile.jpg, movie_app.png, weather_app.png).

They're already declared as an asset directory in pubspec.yaml,
so anything added here is available immediately after `flutter pub get`.
